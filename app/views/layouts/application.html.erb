<!DOCTYPE html>
<html>
  <head>
    <title>PropshaftApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <% if ENV["ESBUILD_MODULES"] %>
      <%= javascript_include_tag "js/modern/application", defer: true, type: "module" %>
    <% else %>
      <%= javascript_include_tag "js/modern/application", defer: true %>
    <% end %>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
